{
  "homepage": "https://michaelcychan.github.io/scales-arpeggios-randomiser/",
  "name": "scales-arpeggios-randomiser",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "jest --coverage",
    "build": "esbuild index.js --bundle  --outfile=bundle.js --watch",
    "predeploy": "npm run build",
    "deploy": "npm run build && gh-pages -d build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "esbuild": "^0.15.9",
    "jest": "^29.0.3",
    "jest-environment-jsdom": "^29.0.3",
    "jest-multiline-matchers": "^0.2.1"
  },
  "jest": {
    "testEnvironment": "jsdom"
  },
  "devDependencies": {
    "gh-pages": "^4.0.0"
  }
}
